{
  "name": "helios-bridge-dapp",
  "version": "1.0.0",
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js --network helios",
    "bridge": "node bridge.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "ethers": "^6.7.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.20.2"
  }
}
